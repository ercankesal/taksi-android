{
  "name": "taksi-android",
  "version": "1.0.0",
  "description": "Güngören Taksi Durağı sürücü uygulaması",
  "main": "index.js",
  "scripts": {
    "build:android": "cap add android && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/geolocation": "^6.0.1",
    "@capacitor/preferences": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}